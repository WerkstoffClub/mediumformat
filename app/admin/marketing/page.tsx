import { PageShell, ComingSoon } from "@/components/admin/PageShell";

export default function MarketingPage() {
  return (
    <PageShell
      title="Marketing"
      description="Newsletter (Listmonk + OpenRouter AI assist), promo codes."
    >
      <ComingSoon phase="MVP-3" />
    </PageShell>
  );
}
